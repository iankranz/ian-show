{
  "name": "ian-show",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "buildian": "tsc && vite build && npx tailwindcss -i ./src/style.css -o ./dist/output.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.0"
  }
}
